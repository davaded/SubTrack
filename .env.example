# ================================
# 核心配置 (REQUIRED / 必选配置)
# ================================

# Database Password / 数据库密码
# Docker 部署时的 PostgreSQL 密码，生成方法：
# openssl rand -base64 32
POSTGRES_PASSWORD="postgres"

# JWT Secret / JWT 密钥
# 用于用户认证的密钥，生成方法：
# openssl rand -hex 32
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Default Admin / 默认管理员账号
# 首次启动时自动创建的管理员账号（如果没有管理员存在）
# ⚠️ 请在首次登录后立即修改密码！
DEFAULT_ADMIN_EMAIL="admin@example.com"
DEFAULT_ADMIN_PASSWORD="admin123456"
DEFAULT_ADMIN_NAME="System Administrator"

# App URL / 应用访问地址
# 开发环境：http://localhost:3000
# 生产环境：https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment / 运行环境
# development | production | test
NODE_ENV="development"

# Webhook Secret / Webhook 安全密钥
# 用于保护定时任务 API 端点 (/api/reminders/check)
# 生成方法同 JWT_SECRET
WEBHOOK_SECRET="your-webhook-secret-change-this"


# ================================
# 通知渠道配置 (OPTIONAL / 可选配置)
# ================================
# 至少配置一个通知渠道，可以同时启用多个

# ---- Email / 邮件通知 (Resend) ----
# 文档：https://resend.com/docs/introduction
# 如果不需要邮件通知，注释掉或删除这两行
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="SubTrack <noreply@yourdomain.com>"

# ---- DingTalk / 钉钉机器人 ----
# 配置文档：见 NOTIFICATION_SETUP.md
# 如果不需要钉钉通知，注释掉或删除这两行
DINGTALK_WEBHOOK="https://oapi.dingtalk.com/robot/send?access_token=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
DINGTALK_SECRET="SECxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# ---- Feishu / 飞书机器人 ----
# 配置文档：见 NOTIFICATION_SETUP.md
# 如果不需要飞书通知，注释掉或删除这两行
FEISHU_WEBHOOK="https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxxxxxxxxxxxxxxxx"
FEISHU_SECRET="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"


# ================================
# 配置说明 / Configuration Guide
# ================================
#
# Docker 部署必选配置 (2 个)：
#   ✅ POSTGRES_PASSWORD  - 数据库密码（生产环境必改）
#   ✅ JWT_SECRET         - JWT 密钥（生产环境必改）
#
# 本地开发额外配置：
#   ✅ DATABASE_URL       - 数据库连接（Docker 部署不需要）
#   ✅ NEXT_PUBLIC_APP_URL - 应用地址
#   ✅ NODE_ENV           - 运行环境
#   ✅ WEBHOOK_SECRET     - Webhook 密钥
#
# 可选配置 (至少选一个通知渠道)：
#   📧 RESEND_API_KEY + EMAIL_FROM       - 邮件通知
#   📱 DINGTALK_WEBHOOK + DINGTALK_SECRET - 钉钉通知
#   📱 FEISHU_WEBHOOK + FEISHU_SECRET     - 飞书通知
#
# 配置示例：
#   - 最小配置：5 个必选项 + 任意 1 个通知渠道
#   - 只用钉钉：5 个必选项 + DINGTALK_*（注释掉 RESEND 和 FEISHU）
#   - 只用飞书：5 个必选项 + FEISHU_*（注释掉 RESEND 和 DINGTALK）
#   - 三个都用：5 个必选项 + 所有通知渠道配置
#
# 详细文档：
#   - 通知配置：./NOTIFICATION_SETUP.md
#   - 邮件配置：./REMINDER_SETUP.md (中文) / ./REMINDER_SETUP_EN.md (English)
#   - 项目文档：./README.md (English) / ./README.zh-CN.md (中文)
