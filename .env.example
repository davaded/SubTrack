# ================================
# 核心配置 (REQUIRED / 必选配置)
# ================================

# Database Password / 数据库密码
# Docker 部署时的 PostgreSQL 密码，生成方法：
# openssl rand -base64 32
POSTGRES_PASSWORD="postgres"

# JWT Secret / JWT 密钥
# 用于用户认证的密钥，生成方法：
# openssl rand -hex 32
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Default Admin / 默认管理员账号
# 首次启动时自动创建的管理员账号（如果没有管理员存在）
# ⚠️ 请在首次登录后立即修改密码！
DEFAULT_ADMIN_EMAIL="admin@example.com"
DEFAULT_ADMIN_PASSWORD="admin123456"
DEFAULT_ADMIN_NAME="System Administrator"


# ================================
# 通知渠道配置 (OPTIONAL / 可选配置)
# ================================
# 如果需要邮件/通知提醒功能，至少配置一个通知渠道

# ---- App URL / 应用访问地址 (仅通知功能需要) ----
# 用于邮件和通知消息中的链接
# 如果不使用邮件/钉钉/飞书通知，可以不配置
# 开发环境：http://localhost:3000
# 生产环境：http://你的服务器IP:3000 或 https://your-domain.com
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ---- Email / 邮件通知 (Resend) ----
# 文档：https://resend.com/docs/introduction
# 如果不需要邮件通知，注释掉或删除这两行
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="SubTrack <noreply@yourdomain.com>"

# ---- DingTalk / 钉钉机器人 ----
# 配置文档：见 NOTIFICATION_SETUP.md
# 如果不需要钉钉通知，注释掉或删除这两行
DINGTALK_WEBHOOK="https://oapi.dingtalk.com/robot/send?access_token=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
DINGTALK_SECRET="SECxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# ---- Feishu / 飞书机器人 ----
# 配置文档：见 NOTIFICATION_SETUP.md
# 如果不需要飞书通知，注释掉或删除这两行
FEISHU_WEBHOOK="https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxxxxxxxxxxxxxxxx"
FEISHU_SECRET="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"


# ================================
# 配置说明 / Configuration Guide
# ================================
#
# 🎯 最小配置（只需 3 项！）：
#   ✅ POSTGRES_PASSWORD       - 数据库密码（生产环境必改）
#   ✅ JWT_SECRET              - JWT 密钥（生产环境必改）
#   ✅ DEFAULT_ADMIN_EMAIL/PASSWORD/NAME - 管理员账号
#
# 🔔 可选配置（如果需要通知提醒功能）：
#   📧 NEXT_PUBLIC_APP_URL + RESEND_API_KEY + EMAIL_FROM - 邮件通知
#   📱 NEXT_PUBLIC_APP_URL + DINGTALK_WEBHOOK + DINGTALK_SECRET - 钉钉通知
#   📱 NEXT_PUBLIC_APP_URL + FEISHU_WEBHOOK + FEISHU_SECRET - 飞书通知
#
# 💡 配置示例：
#   - 最简单：只配置上面 3 项必选配置（不使用通知功能）
#   - 使用邮件：3 项必选 + NEXT_PUBLIC_APP_URL + RESEND_* + EMAIL_FROM
#   - 使用钉钉：3 项必选 + NEXT_PUBLIC_APP_URL + DINGTALK_*
#   - 全部功能：3 项必选 + NEXT_PUBLIC_APP_URL + 所有通知渠道
#
# 📖 详细文档：
#   - 快速开始：./QUICKSTART.md
#   - 通知配置：./NOTIFICATION_SETUP.md
#   - 邮件配置：./REMINDER_SETUP.md (中文) / ./REMINDER_SETUP_EN.md (English)
#   - 项目文档：./README.md (English) / ./README.zh-CN.md (中文)
